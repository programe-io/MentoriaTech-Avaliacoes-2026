// Declarando uma variável
let mensagem = "Olá, mundo!";

// Exibindo a mensagem no console do navegador
console.log(mensagem);

// Verificando uma condição simples
let hora = 12;
if (hora < 12) {
    console.log("Bom dia!");
} else {
    console.log("Boa tarde!");
}

