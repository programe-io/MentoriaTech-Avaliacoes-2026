// Seleciona o botão
const botao = document.querySelector("button");

// Adiciona evento de clique
botao.addEventListener("click", function () {
  alert("Código enviado para correção com sucesso!");
  });