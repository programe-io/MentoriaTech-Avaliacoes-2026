// Mensagem ao carregar a página
window.onload = function () {
  alert("Seu ambiente de envio de códigos foi carregado com sucesso!");
  };

  // Função de confirmação de envio
  function enviarCodigo() {
    alert("Código enviado para correção!");
    }