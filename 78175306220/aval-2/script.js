const botao = document.getElementById("btn");
const resultado = document.getElementById("resultado");

botao.addEventListener("click", function () {
    resultado.innerHTML = "Parabéns! Seu código JavaScript está funcionando.";
});